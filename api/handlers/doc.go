// Package handlers contains functions that
// setup API endpoint handlers.
// by convention , every endpoint should be defined
// in a separate file and have an initialization function that
// would register itself with the API registry
package handlers
